#! /usr/bin/env python3

# Kept for `python setup.py <command>` compatibility; all metadata lives in
# pyproject.toml. Use `pip install .` for a normal install.

from setuptools import setup

setup()
